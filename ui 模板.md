# 二维图形绘图系统 Web UI 展示页面 - 提示词

## 整体需求

创建一个现代化的二维图形绘图系统 Web UI 展示页面，采用响应式布局展示应用的核心功能。页面支持浅色/深色主题切换，具备交互式 Canvas 绘图演示、工具展示、作品画廊等完整功能。设计风格简洁专业，注重用户体验和视觉美感。

## 技术要求

- 纯 HTML + CSS + JavaScript 实现
- Canvas API 实现绘图功能
- CSS 变量系统实现主题切换
- 响应式网格布局（Grid/Flexbox）
- 平滑动画过渡
- 毛玻璃效果（backdrop-filter）

## 核心功能模块

### 1. 主题系统
- 浅色主题（默认）
- 深色主题
- 一键切换，全局响应
- 使用 CSS 变量管理配色

### 2. 导航系统
- 顶部固定导航栏
- 功能分类标签栏
- 移动端底部导航栏

### 3. 内容展示
- 功能卡片网格布局
- 交互式 Canvas 演示区
- 作品画廊展示
- 工具介绍区域

### 4. 交互功能
- 实时绘图演示
- 工具切换
- 颜色选择
- 撤销/重做
- 导出图片

## CSS 变量定义

### 浅色主题（默认）

```css
--primary-color: #2563eb        // 主蓝色
--primary-hover: #1d4ed8        // 悬停蓝
--bg-color: #FFFFFF             // 背景白
--secondary-bg: #F8FAFC         // 次要背景灰
--text-primary: #0F172A         // 主文字黑
--text-secondary: #64748B       // 次要文字灰
--text-light: #94A3B8           // 浅色文字
--border-color: #E2E8F0         // 边框灰
--shadow: 0 2px 8px rgba(0,0,0,0.08)           // 基础阴影
--shadow-hover: 0 8px 24px rgba(0,0,0,0.15)   // 悬停阴影
--radius: 12px                  // 圆角
--radius-small: 8px             // 小圆角
--nav-height: 60px              // 导航栏高度
--toolbar-height: 50px          // 工具栏高度
```

### 深色主题

```css
--bg-color: #0F172A             // 背景深灰
--secondary-bg: #1E293B         // 次要背景
--text-primary: #F1F5F9         // 主文字浅色
--text-secondary: #CBD5E1       // 次要文字
--text-light: #94A3B8           // 浅色文字
--border-color: #334155         // 边框深灰
--shadow: 0 2px 8px rgba(0,0,0,0.3)
--shadow-hover: 0 8px 24px rgba(0,0,0,0.5)
```

## 详细布局设计

### 1. 顶部导航栏

**样式规范：**
- 固定定位：position: fixed; top: 0
- 高度：60px
- 背景：半透明白色 rgba(255,255,255,0.9) + 毛玻璃效果
- 底部边框：1px 浅灰色
- 内边距：0 20px
- z-index: 1000

**组成元素：**

**Logo**
- 位置：最左侧
- 内容："🎨 绘图系统"
- 字体大小：24px
- 颜色：主蓝色 #2563eb
- 字重：bold
- 右边距：20px

**导航菜单**
- 布局：flex，间距 30px
- 菜单项：首页、功能、画廊、文档
- 字体大小：15px
- 颜色：次要文字色
- 悬停颜色：主文字色
- 激活状态：主蓝色，字重 600

**用户操作区**
- 布局：flex，间距 15px
- 包含元素：
  - 主题切换按钮
    - 尺寸：36px × 36px
    - 圆形：border-radius: 50%
    - 图标：🌙（浅色）/ ☀️（深色）
    - 字体大小：20px
    - 悬停效果：次要背景色
  - 开始绘图按钮
    - 内边距：8px 20px
    - 背景：主蓝色渐变
    - 颜色：白色
    - 圆角：8px
    - 悬停效果：背景变深

### 2. Hero 区域

**样式规范：**
- 上边距：60px（导航栏高度）
- 内边距：80px 20px
- 背景：渐变色 linear-gradient(135deg, #667eea 0%, #764ba2 100%)
- 文本居中
- 颜色：白色

**内容结构：**
- 主标题："专业的二维图形绘图工具"
  - 字体大小：48px
  - 字重：700
  - 下边距：20px
- 副标题："支持点、线、矩形、圆形、多边形等多种图形绘制"
  - 字体大小：20px
  - 透明度：0.9
  - 下边距：40px
- 操作按钮组：
  - 主按钮："立即体验"
    - 内边距：12px 32px
    - 背景：白色
    - 颜色：主蓝色
    - 圆角：8px
    - 字体大小：16px
    - 字重：600
  - 次按钮："查看文档"
    - 内边距：12px 32px
    - 背景：透明
    - 边框：2px solid white
    - 颜色：白色
    - 圆角：8px

### 3. 功能展示区域

**容器样式：**
- 内边距：60px 20px
- 最大宽度：1200px
- 居中显示

**标题：**
- "核心功能"
- 字体大小：36px
- 字重：700
- 文本居中
- 下边距：50px

**功能卡片网格：**
- 布局：Grid
- 桌面端（≥ 1024px）：3 列
- 平板端（≥ 768px）：2 列
- 移动端（< 768px）：1 列
- 列间距：30px
- 行间距：30px

**功能卡片设计：**
- 背景：主背景色
- 圆角：16px
- 内边距：30px
- 阴影：基础阴影
- 边框：1px solid 边框色
- 悬停效果：
  - 向上移动 8px
  - 阴影增强
  - 边框变为主蓝色
  - 过渡：0.3s ease

**卡片内容：**
- 图标：
  - 字体大小：48px
  - 下边距：20px
- 标题：
  - 字体大小：20px
  - 字重：600
  - 颜色：主文字色
  - 下边距：12px
- 描述：
  - 字体大小：15px
  - 行高：1.6
  - 颜色：次要文字色

**功能列表：**
1. 基础图形绘制
   - 图标：📐
   - 描述：支持点、线、矩形、圆形、多边形等基础图形
2. 自由画笔
   - 图标：🖌️
   - 描述：普通画笔、马克笔、书法笔等多种笔触
3. 图形编辑
   - 图标：✏️
   - 描述：选择、移动、缩放、旋转图形
4. 颜色管理
   - 图标：🎨
   - 描述：自定义颜色、线宽、填充样式
5. 撤销重做
   - 图标：↩️
   - 描述：完整的撤销/重做历史记录
6. 导出功能
   - 图标：💾
   - 描述：导出为 PNG、JSON 格式

### 4. 交互式演示区域

**容器样式：**
- 内边距：80px 20px
- 背景：次要背景色
- 最大宽度：1400px
- 居中显示

**标题：**
- "在线体验"
- 字体大小：36px
- 字重：700
- 文本居中
- 下边距：50px

**演示区布局：**
- 布局：Grid
- 桌面端：左侧工具栏（200px）+ 画布区域（flex: 1）
- 移动端：垂直布局

**工具栏设计：**
- 背景：主背景色
- 圆角：12px
- 内边距：20px
- 阴影：基础阴影
- 边框：1px solid 边框色

**工具按钮：**
- 尺寸：48px × 48px
- 圆角：8px
- 背景：透明
- 边框：2px solid 边框色
- 下边距：12px
- 悬停效果：
  - 背景：次要背景色
  - 边框：主蓝色
- 激活状态：
  - 背景：主蓝色
  - 颜色：白色

**工具列表：**
- 选择工具：🖱️
- 点：⚫
- 直线：📏
- 矩形：▭
- 圆形：⭕
- 多边形：⬡
- 画笔：🖌️
- 橡皮擦：🧹

**颜色选择器：**
- 标题："颜色"，14px，灰色
- 当前颜色预览：
  - 尺寸：48px × 48px
  - 圆角：8px
  - 边框：2px solid 边框色
  - 点击打开颜色选择器

**线宽选择器：**
- 标题："线宽"，14px，灰色
- 滑块：
  - 宽度：100%
  - 高度：4px
  - 背景：边框色
  - 滑块：12px × 12px，圆形，主蓝色

**画布区域：**
- 背景：白色
- 圆角：12px
- 阴影：基础阴影
- 边框：1px solid 边框色
- 最小高度：500px
- Canvas：
  - 宽度：100%
  - 高度：100%
  - 光标：根据工具变化

**操作按钮组：**
- 位置：画布下方
- 布局：flex，space-between
- 左侧：
  - 撤销按钮：↩️
  - 重做按钮：↪️
  - 清空按钮：🗑️
- 右侧：
  - 导出 PNG 按钮
  - 保存 JSON 按钮

### 5. 作品画廊区域

**容器样式：**
- 内边距：80px 20px
- 最大宽度：1400px
- 居中显示

**标题：**
- "作品展示"
- 字体大小：36px
- 字重：700
- 文本居中
- 下边距：50px

**画廊网格：**
- 布局：Grid
- 桌面端（≥ 1024px）：4 列
- 平板端（≥ 768px）：3 列
- 移动端（< 768px）：2 列
- 列间距：20px
- 行间距：20px

**作品卡片：**
- 背景：主背景色
- 圆角：12px
- 阴影：基础阴影
- 溢出：隐藏
- 悬停效果：
  - 向上移动 4px
  - 阴影增强
  - 过渡：0.3s ease

**卡片结构：**
- 图片区域：
  - 宽度：100%
  - 纵横比：1:1
  - 对象适应：cover
  - 背景：次要背景色
  - 悬停效果：scale(1.05)
- 信息区域：
  - 内边距：15px
  - 标题：
    - 字体大小：14px
    - 字重：600
    - 颜色：主文字色
    - 下边距：8px
  - 元信息：
    - 字体大小：12px
    - 颜色：浅色文字
    - 包含：创建时间、图形数量

### 6. 模态框（详情查看）

**遮罩层：**
- 固定定位：全屏覆盖
- 背景：rgba(0,0,0,0.7)
- z-index: 2000
- 默认隐藏：display: none
- 激活时：display: flex，居中对齐
- 出现动画：fadeIn，0.3s

**内容容器：**
- 最大宽度：1200px
- 最大高度：90vh
- 背景：主背景色
- 圆角：16px
- 可滚动
- 出现动画：slideUp，0.3s

**关闭按钮：**
- 绝对定位：右上角（20px, 20px）
- 尺寸：40px × 40px
- 圆形
- 背景：rgba(0,0,0,0.5)
- 颜色：白色
- 图标：✕，24px
- z-index: 10
- 悬停效果：
  - 背景变深：rgba(0,0,0,0.7)
  - 旋转 90 度
  - 过渡：0.3s

**内容布局：**
- 桌面端：左侧画布（70%）+ 右侧信息（30%）
- 移动端：垂直布局

**画布区域：**
- 背景：白色
- 内边距：20px
- Canvas 居中显示

**信息区域：**
- 内边距：30px
- 标题：
  - 字体大小：24px
  - 字重：700
  - 下边距：20px
- 详细信息：
  - 创建时间
  - 图形数量
  - 使用的工具
  - 文件大小
- 操作按钮：
  - 下载 PNG
  - 下载 JSON
  - 在编辑器中打开

### 7. 底部导航栏（移动端）

**显示条件：**
- 仅在移动端显示（< 768px）
- 桌面端隐藏

**样式规范：**
- 固定定位：bottom: 0
- 高度：60px
- 背景：主背景色
- 顶部边框：1px 浅灰色
- 布局：flex，space-around
- z-index: 1000
- 底部安全区域适配：padding-bottom: env(safe-area-inset-bottom)

**导航项：**
- 布局：垂直排列，居中对齐
- 间距：4px
- 颜色：浅色文字
- 图标大小：24px
- 文字大小：11px
- 过渡：0.3s
- 激活状态：颜色：主蓝色

**导航项目：**
- 首页：🏠
- 功能：🛠️
- 绘图：✏️（居中，向上偏移 8px，28px 大小）
- 画廊：🖼️
- 我的：👤

### 8. 页脚

**样式规范：**
- 内边距：40px 20px
- 背景：次要背景色
- 边框顶部：1px solid 边框色

**内容布局：**
- 最大宽度：1200px
- 居中显示
- 布局：Grid
- 桌面端：4 列
- 移动端：1 列

**列内容：**
1. 关于
   - 应用简介
   - 版本信息
2. 功能
   - 功能列表链接
3. 资源
   - 文档
   - 教程
   - API
4. 联系
   - GitHub
   - 反馈

**版权信息：**
- 文本居中
- 字体大小：14px
- 颜色：浅色文字
- 上边距：30px

## 动画定义

### 1. fadeIn（淡入）

```css
@keyframes fadeIn {
  from { opacity: 0; }
  to { opacity: 1; }
}
```

### 2. slideUp（向上滑入）

```css
@keyframes slideUp {
  from {
    opacity: 0;
    transform: translateY(50px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}
```

### 3. spin（旋转）

```css
@keyframes spin {
  from { transform: rotate(0deg); }
  to { transform: rotate(360deg); }
}
```

### 4. pulse（脉冲）

```css
@keyframes pulse {
  0%, 100% { transform: scale(1); }
  50% { transform: scale(1.05); }
}
```

## JavaScript 功能实现

### 1. 数据结构

**图形数据对象：**

```javascript
{
  id: 数字,
  type: '图形类型', // 'point', 'line', 'rect', 'circle', 'polygon', 'path'
  properties: {
    x: 数字,
    y: 数字,
    width: 数字,
    height: 数字,
    radius: 数字,
    points: 数组,
    strokeColor: '颜色',
    strokeWidth: 数字,
    fillColor: '颜色',
    opacity: 数字
  },
  timestamp: 时间戳
}
```

### 2. 核心功能

**Canvas 绘图：**
- 初始化 Canvas 上下文
- 设置画布尺寸（响应式）
- 实现各种图形绘制方法
- 实现画笔路径绘制
- 实现橡皮擦功能

**工具管理：**
- 工具切换
- 工具状态管理
- 鼠标事件处理（mousedown, mousemove, mouseup）
- 触摸事件处理（移动端）

**图形管理：**
- 添加图形到列表
- 删除图形
- 选择图形
- 移动图形
- 修改图形属性

**撤销/重做：**
- 维护历史记录栈
- 撤销操作
- 重做操作
- 限制历史记录数量

**导出功能：**
- 导出为 PNG 图片
- 导出为 JSON 数据
- 触发下载

**主题切换：**
- 切换 data-theme 属性
- 更新按钮图标
- CSS 变量自动响应
- 保存用户偏好到 localStorage

### 3. 事件监听

- Canvas 鼠标/触摸事件
- 工具按钮点击
- 颜色选择器变化
- 线宽滑块变化
- 撤销/重做按钮
- 清空按钮
- 导出按钮
- 主题切换按钮
- 窗口大小变化（响应式）

### 4. 性能优化

**Canvas 优化：**
- 使用 requestAnimationFrame
- 避免频繁重绘
- 使用离屏 Canvas
- 图层管理

**事件优化：**
- 防抖和节流
- 事件委托
- 移除不必要的监听器

**内存优化：**
- 限制历史记录数量
- 及时清理不用的对象
- 图片懒加载

## 响应式设计

### 移动端（< 768px）
- 单列布局
- 显示底部导航
- 简化工具栏
- 触摸优化
- 减少间距和内边距

### 平板端（≥ 768px）
- 2-3 列布局
- 隐藏底部导航
- 完整工具栏
- 正常尺寸

### 桌面端（≥ 1024px）
- 3-4 列布局
- 最大宽度限制
- 完整功能
- 悬停效果

## 配色方案总结

**主色调：**
- 主蓝色：#2563eb
- 悬停蓝：#1d4ed8
- 激活蓝：#1e40af

**浅色主题：**
- 背景：#FFFFFF / #F8FAFC
- 文字：#0F172A / #64748B / #94A3B8
- 边框：#E2E8F0

**深色主题：**
- 背景：#0F172A / #1E293B
- 文字：#F1F5F9 / #CBD5E1 / #94A3B8
- 边框：#334155

**功能色：**
- 成功：#10B981（绿色）
- 警告：#F59E0B（橙色）
- 错误：#EF4444（红色）
- 信息：#3B82F6（蓝色）

## 特色功能

- **实时绘图演示**：用户可以直接在网页上体验绘图功能
- **平滑过渡**：所有交互都有过渡动画
- **主题切换**：一键切换浅色/深色模式
- **响应式布局**：适配所有设备尺寸
- **Canvas 优化**：高性能绘图体验
- **完整功能**：支持所有核心绘图功能
- **导出功能**：支持 PNG 和 JSON 格式导出
- **作品展示**：展示用户创作的作品

## 示例数据

**功能卡片数据（6个）：**
1. 基础图形绘制
2. 自由画笔
3. 图形编辑
4. 颜色管理
5. 撤销重做
6. 导出功能

**作品画廊数据（12个）：**
- 几何图案
- 建筑草图
- 流程图
- 思维导图
- 手绘插画
- 技术图纸
- UI 原型
- 数据可视化
- 艺术创作
- 教学示例
- 游戏设计
- 其他创意作品

每个作品包含：预览图、标题、创建时间、图形数量、JSON 数据
