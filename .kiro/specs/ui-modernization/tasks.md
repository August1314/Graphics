# Implementation Plan

- [x] 1. 创建样式系统基础设施
  - 创建 `app/ui/styles/` 目录结构
  - 实现 `StyleConfig` 数据类，定义所有样式常量（颜色、字体、间距、圆角）
  - 实现 `ThemeManager` 类，管理主题切换和样式生成
  - 实现 `generate_stylesheet()` 方法，生成完整的QSS样式表
  - _Requirements: 1.1, 1.2, 5.1, 5.2_

- [x] 2. 实现配色方案和样式常量
  - 定义主色系（PRIMARY, PRIMARY_HOVER, PRIMARY_ACTIVE, PRIMARY_LIGHT）
  - 定义中性色系（BACKGROUND, SURFACE, BORDER, DIVIDER）
  - 定义文字色系（TEXT_PRIMARY, TEXT_SECONDARY, TEXT_DISABLED）
  - 定义语义色系（SUCCESS, WARNING, ERROR, INFO）
  - 定义字体系统（FONT_FAMILY, FONT_SIZE_*, FONT_WEIGHT_*）
  - 定义间距系统（SPACING_XS到SPACING_2XL）
  - 定义圆角系统（RADIUS_SM到RADIUS_FULL）
  - _Requirements: 5.1, 5.5_

- [x] 3. 创建完整的QSS样式表
  - 编写主窗口样式（背景色）
  - 编写工具栏样式（背景、边框、按钮样式）
  - 编写停靠窗口样式（标题栏、边框）
  - 编写按钮样式（普通、悬停、按下、禁用状态）
  - 编写输入控件样式（SpinBox, ComboBox, 聚焦效果）
  - 编写滑块样式（轨道、手柄）
  - 编写状态栏样式
  - 编写菜单栏和菜单样式
  - _Requirements: 1.1, 1.2, 1.3, 1.4, 5.2, 5.3_

- [x] 4. 重构工具栏布局
  - 移除"图形"和"画笔"下拉按钮
  - 将所有工具按钮直接展示在工具栏上
  - 确认喷枪工具已完全移除（检查代码中无 `action_brush_spray` 引用）
  - 调整工具按钮尺寸为36x36px，图标尺寸为20x20px
  - 在工具按钮组和快速样式面板之间添加分隔线
  - 更新工具按钮的样式类名以应用新样式
  - _Requirements: 1.5, 1.6, 1.7_

- [x] 5. 优化快速样式面板
  - 重新设计颜色按钮，显示32x32px色块预览
  - 优化线宽和线型控件的样式和布局
  - 添加视觉分隔，使用卡片式背景
  - 调整控件间距和对齐方式
  - 应用统一的圆角和边框样式
  - _Requirements: 1.7, 2.1, 2.4, 2.5_

- [x] 6. 创建现代化颜色选择器组件
  - 创建 `ColorPickerWidget` 类
  - 实现当前颜色预览区域（大色块显示）
  - 实现快速颜色预设网格（8种常用颜色）
  - 集成Qt标准颜色对话框作为自定义颜色选择
  - 添加颜色选择的信号和槽
  - 应用卡片式样式和圆角设计
  - _Requirements: 2.1, 2.2, 2.3, 2.4, 2.5_

- [x] 7. 集成颜色选择器到工具栏
  - 替换工具栏中的颜色按钮为新的颜色选择器
  - 连接颜色变化信号到画笔工具
  - 更新颜色按钮的视觉样式
  - 测试颜色选择和实时预览功能
  - _Requirements: 2.1, 2.4, 6.1, 6.3_

- [x] 8. 简化属性面板布局
  - 移除 `QTabWidget`，改为单页面滚动布局
  - 实现卡片式属性组布局
  - 为每个属性组添加标题和分组样式
  - 调整标签文字大小为14px
  - 统一输入控件高度为32px
  - 应用卡片阴影和圆角样式
  - _Requirements: 3.1, 3.2, 3.3, 3.6_

- [x] 9. 添加属性面板空状态提示
  - 创建空状态提示组件（显示"请选择一个图形"）
  - 在未选中图形时显示空状态
  - 在选中图形时显示属性编辑界面
  - 添加友好的图标和提示文字
  - 应用统一的样式
  - _Requirements: 3.5_

- [x] 10. 优化主窗口布局
  - 移除"文件"停靠面板（`io_dock`）
  - 将保存/加载功能整合到菜单栏
  - 调整画布区域边距为0（全出血）
  - 优化停靠面板的默认尺寸和位置
  - 应用统一的背景色
  - _Requirements: 4.1, 4.2, 4.3, 4.5_

- [x] 11. 增强状态栏信息显示
  - 添加当前工具名称显示
  - 添加鼠标坐标显示（实时更新）
  - 添加缩放比例显示
  - 添加对象数量显示
  - 使用分隔符"|"分隔不同信息
  - 应用状态栏样式（背景色、字体、高度）
  - _Requirements: 4.6, 6.3_

- [x] 12. 优化菜单栏结构
  - 简化菜单项，移除不常用功能
  - 为菜单项添加图标
  - 优化快捷键提示显示
  - 使用分隔线合理分组
  - 应用菜单样式（背景、悬停效果）
  - _Requirements: 7.1, 7.2, 7.3, 7.4_

- [x] 13. 应用样式到主窗口
  - 在 `MainWindow.__init__()` 中初始化 `ThemeManager`
  - 生成QSS样式表
  - 使用 `setStyleSheet()` 应用样式到主窗口
  - 添加样式加载错误处理
  - 测试样式是否正确应用到所有组件
  - _Requirements: 5.1, 5.2, 5.3, 5.4, 5.5, 5.6_

- [x] 14. 优化图标系统
  - 更新 `IconProvider` 以支持统一的20x20px图标尺寸
  - 确保所有图标使用统一的颜色（TEXT_PRIMARY）
  - 优化图标的抗锯齿渲染
  - 添加图标缓存机制提升性能
  - 测试所有工具图标的显示效果
  - _Requirements: 5.5, 10.3_

- [x] 15. 实现交互反馈优化
  - 为所有按钮添加悬停效果（通过QSS）
  - 为可交互元素添加手型光标
  - 优化工具切换时的状态栏更新
  - 添加按钮按下的视觉反馈
  - 测试所有交互的响应速度
  - _Requirements: 6.1, 6.2, 6.3, 10.1_

- [x] 16. 实现响应式布局基础
  - 添加窗口尺寸变化监听
  - 实现工具栏的自适应布局逻辑
  - 实现属性面板的自适应显示
  - 测试不同窗口尺寸下的布局效果
  - _Requirements: 8.1, 8.2, 8.3, 8.4_

- [x] 17. 优化可访问性
  - 为所有工具按钮设置 `accessibleName` 和 `accessibleDescription`
  - 确保Tab键导航顺序正确
  - 添加焦点指示器样式（通过QSS）
  - 验证文字和背景的对比度符合WCAG AA标准
  - 测试键盘导航功能
  - _Requirements: 9.1, 9.2, 9.3, 9.4, 9.5_

- [x] 18. 性能优化
  - 实现样式表缓存机制
  - 预加载常用图标
  - 优化QSS选择器，避免过度复杂的规则
  - 测量工具切换响应时间（目标<100ms）
  - 测量属性更新响应时间（目标60fps）
  - _Requirements: 10.1, 10.2, 10.3, 10.4, 10.5_

- [x] 19. 创建样式文档和示例
  - 创建 `STYLE_GUIDE.md` 文档
  - 记录所有样式常量和使用方法
  - 提供组件样式示例代码
  - 添加样式自定义指南
  - 创建样式预览工具（可选）
  - _Requirements: 5.1, 5.2, 5.3, 5.4, 5.5, 5.6_

- [x] 20. 集成测试和验收
  - 测试所有工具的视觉样式
  - 测试颜色选择器功能
  - 测试属性面板的所有交互
  - 测试响应式布局
  - 测试可访问性功能
  - 进行视觉回归测试（截图对比）
  - 收集用户反馈并进行最终调整
  - _Requirements: 所有需求_
