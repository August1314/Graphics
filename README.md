# 二维图形绘图系统（最小可运行版本）

## 环境准备

```bash
conda activate pnt
```

## 运行

```bash
python -m app.main
```

- 主窗口包含：菜单栏、中心画布（支持滚轮缩放、拖拽平移）。
- 画布已放置矩形与圆形示例，支持选择与拖动。
- 菜单 文件 → 导出PNG 可将当前视图导出为 PNG。


```

如未激活环境，请先参考规划文档创建 `pnt` 环境并安装依赖。

