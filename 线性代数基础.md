# 函数的 L2 范数

### 定义
给定可测域 \(\Omega\) 与测度 \(\mu\)，函数 \(f: \Omega \to \mathbb{C}\) 的 \(L^2\) 范数定义为
\[\|f\|_{L^2(\Omega)} = \left( \int_{\Omega} |f(x)|^2\, d\mu(x) \right)^{1/2}.\]
当 \(\mu\) 为勒贝格测度时，常写作
\[\|f\|_{2} = \left( \int_{\Omega} |f(x)|^2\, dx \right)^{1/2}.\]

### 常见情形
- 计算域为区间 \([a,b]\): \[\|f\|_{2} = \left( \int_{a}^{b} |f(x)|^2\, dx \right)^{1/2}.\]
- 加权情形（权函数 \(w(x) > 0\)）: \[\|f\|_{L^2_w} = \left( \int_{\Omega} |f(x)|^2 w(x)\, dx \right)^{1/2}.\]
- 离散采样（向量/序列）: 设 \(\mathbf{f} = (f_1,\dots,f_n)\)，则
  \[\|\mathbf{f}\|_2 = \left( \sum_{i=1}^n |f_i|^2 \right)^{1/2}.\]
- 随机变量 \(X\)（二阶可积）: \[\|X\|_{2} = \left( \mathbb{E}\,|X|^2 \right)^{1/2}.\]

### 性质
- 非负定性: \(\|f\|_2 \ge 0\)，且 \(\|f\|_2 = 0\) 当且仅当 \(f=0\) 几乎处处。
- 齐次性: \(\|\alpha f\|_2 = |\alpha|\,\|f\|_2\)。
- 三角不等式（Minkowski）: \(\|f+g\|_2 \le \|f\|_2 + \|g\|_2\)。
- Cauchy–Schwarz: \(|\langle f,g\rangle| \le \|f\|_2\,\|g\|_2\)。

### 与内积空间
在 \(L^2(\Omega)\) 中内积定义为
\[\langle f,g\rangle = \int_{\Omega} f(x)\,\overline{g(x)}\, d\mu(x),\]
且 \(\|f\|_2 = \sqrt{\langle f,f\rangle}\)。若 \(f\) 与 \(g\) 正交（\(\langle f,g\rangle=0\)），则满足毕达哥拉斯定理
\[\|f+g\|_2^2 = \|f\|_2^2 + \|g\|_2^2.\]

### 示例
- \(f(x)=x\) 在 \([0,1]\) 上: \[\|f\|_2 = \left(\int_0^1 x^2\,dx\right)^{1/2} = \sqrt{\tfrac{1}{3}}.\]
- 向量 \((3,-4,12)\): \[\|(3,-4,12)\|_2 = \sqrt{3^2+(-4)^2+12^2} = \sqrt{169} = 13.\]

### 备注
- \(L^2\) 空间由“平方可积”的函数等价类构成（几乎处处相等视为同一元素）。
- 归一化常用：\(\displaystyle f_{\mathrm{norm}} = f/\|f\|_2\)，使得 \(\|f_{\mathrm{norm}}\|_2=1\)。

# 向量外积
给定 \(\mathbb{R}^3\) 中的向量 \(\mathbf{a},\mathbf{b}\)，它们的外积（叉积）定义为一个与两者都正交的向量 \(\mathbf{a}\times\mathbf{b}\)。

### 代数定义（行列式形式）
设 \(\mathbf{a}=(a_x,a_y,a_z),\; \mathbf{b}=(b_x,b_y,b_z)\)，则


\[\mathbf{a}\times\mathbf{b} =
\begin{vmatrix}
\mathbf{i} & \mathbf{j} & \mathbf{k} \\
a_x & a_y & a_z \\
b_x & b_y & b_z
\end{vmatrix} = \big(a_y b_z - a_z b_y\big)\,\mathbf{i} - \big(a_x b_z - a_z b_x\big)\,\mathbf{j} + \big(a_x b_y - a_y b_x\big)\,\mathbf{k}.\]



### 几何意义
- \(\mathbf{a}\times\mathbf{b}\) 的方向由右手法则确定，并且与 \(\mathbf{a},\mathbf{b}\) 都正交。
- 长度为两向量张成的平行四边形面积：
  \[\|\mathbf{a}\times\mathbf{b}\| = \|\mathbf{a}\|\,\|\mathbf{b}\|\,\sin\theta,\]
  其中 \(\theta\) 是 \(\mathbf{a}\) 与 \(\mathbf{b}\) 的夹角（取 \(0\le \theta \le \pi\)）。

### 基本性质
- 反对称：\(\mathbf{a}\times\mathbf{b} = -\,\mathbf{b}\times\mathbf{a}\)，且 \(\mathbf{a}\times\mathbf{a}=\mathbf{0}\)。
- 线性性：对任意标量 \(\alpha,\beta\)，\((\alpha\mathbf{a}+\beta\mathbf{b})\times\mathbf{c} = \alpha(\mathbf{a}\times\mathbf{c}) + \beta(\mathbf{b}\times\mathbf{c})\)。
- 与点积的正交性：\((\mathbf{a}\times\mathbf{b})\cdot\mathbf{a}=0\)，\((\mathbf{a}\times\mathbf{b})\cdot\mathbf{b}=0\)。
- 与点积的关系（混合积）：\(\mathbf{a}\cdot(\mathbf{b}\times\mathbf{c})\) 等于由 \(\mathbf{a},\mathbf{b},\mathbf{c}\) 构成的平行六面体体积的有向值。

### 常用恒等式
- 三重积展开（BAC–CAB 公式）：
  \[\mathbf{a}\times(\mathbf{b}\times\mathbf{c}) = \mathbf{b}\,(\mathbf{a}\cdot\mathbf{c}) - \mathbf{c}\,(\mathbf{a}\cdot\mathbf{b}).\]
- 范数关系：
  \[\|\mathbf{a}\times\mathbf{b}\|^2 = \|\mathbf{a}\|^2\,\|\mathbf{b}\|^2 - (\mathbf{a}\cdot\mathbf{b})^2.\]

### 示例
设 \(\mathbf{a}=(1,2,3),\; \mathbf{b}=(4,5,6)\)，则
\[\mathbf{a}\times\mathbf{b} =
\begin{vmatrix}
\mathbf{i} & \mathbf{j} & \mathbf{k} \\
1 & 2 & 3 \\
4 & 5 & 6
\end{vmatrix}
= (2\cdot 6 - 3\cdot 5)\,\mathbf{i} - (1\cdot 6 - 3\cdot 4)\,\mathbf{j} + (1\cdot 5 - 2\cdot 4)\,\mathbf{k}
= (-3,\; 6,\; -3).\]

# 计算机图形学中的三角形
三角形是栅格化渲染的基本图元，具有拓扑简单、插值稳定、完全由仿射/透视投影保持直线等特点。

### 表示与法向
顶点：\(\mathbf{v}_i=(x_i,y_i,z_i,1)^T\)（齐次坐标）。
在 3D 中，面法向（未归一化）：
\[\mathbf{n} = (\mathbf{b}-\mathbf{a})\times(\mathbf{c}-\mathbf{a}).\]
投影到屏幕后用于背面剔除的有向面积（2D, z 分量）：
\[A_2 = (x_b-x_a)(y_c-y_a) - (x_c-x_a)(y_b-y_a).\]

—— 更详细说明 ——
- 坐标空间与变换（右乘记号）：对象点从模型到屏幕的典型链路
  \[\mathbf{v}_\text{clip} = M_\text{proj}\, M_\text{view}\, M_\text{model}\, \mathbf{v}_\text{obj}.\]
  齐次除法：\(\mathbf{v}_\text{ndc}=(x/w, y/w, z/w)\)，再经视口映射得到屏幕坐标。

- 面法向与顶点法向：
  - 面法向用于平面三角形的几何测试、平面着色（flat shading）。
  - 顶点法向通常由相邻三角面法向加权平均获得，用于光滑着色（Gouraud/Phong）。常见权重：
    - 面积权重：\(\mathbf{n}_v \propto \sum_{f\ni v} A_f\, \mathbf{n}_f\)。
    - 角度权重：\(\mathbf{n}_v \propto \sum_{f\ni v} \theta_{f,v}\, \mathbf{n}_f\)。
  最终需归一化：\(\hat{\mathbf{n}} = \mathbf{n}/\|\mathbf{n}\|\)。

- 法向的正确变换（非均匀缩放时尤重要）：
  若顶点位置按仿射矩阵 \(M\) 变换（常取 \(M = M_\text{view} M_\text{model}\) 的 3×3 线性部分），则法向应按逆转置变换：
  \[\mathbf{n}' \propto (M^{-1})^{\!T} \mathbf{n},\quad \hat{\mathbf{n}}' = \frac{\mathbf{n}'}{\|\mathbf{n}'\|}.\]
  原因：要求 \(\mathbf{n}'\cdot (M\,\Delta\mathbf{x})=0\) 对所有切向位移 \(\Delta\mathbf{x}\) 成立。

- 归一化与数值稳健：
  - 计算面法向时若 \(\| (\mathbf{b}-\mathbf{a})\times(\mathbf{c}-\mathbf{a}) \| \approx 0\) 表示三点几乎共线，三角形退化，需剔除或特别处理（避免除零/面积过小）。
  - 每次变换后重新单位化法向，以保持光照计算稳定。

- 绘制顺序与手性（winding & handedness）：
  - 屏幕空间的顶点绕序（顺/逆时针）决定背面剔除结果，需与投影矩阵的手性约定一致。
  - 若镜像（负尺度）使手性改变，应调整绕序或剔除设置以保持一致。

- 切线空间（用于法线贴图，简述）：
  对每个顶点构建正交基 \((\mathbf{t},\mathbf{b},\mathbf{n})\)（切线/副切线/法向），将切线空间法线 \(\mathbf{n}_\text{TS}\) 变换到世界或视空间：
  \[\mathbf{n}_\text{WS} = TBN\,\mathbf{n}_\text{TS},\quad TBN=[\mathbf{t}\;\mathbf{b}\;\mathbf{n}],\]
  之后再归一化并用于光照。

### 面积与定向
面积 \(A = \tfrac{1}{2}|A_2|\)。符号 \(\operatorname{sign}(A_2)\) 给出屏幕空间的顺/逆时针定向，常用于背面剔除。

### 重心（Barycentric）坐标
设屏幕空间点 \(\mathbf{p}=(x,y)\)，定义边函数（取 z 分量）：
\[E_{ab}(\mathbf{p}) = (\mathbf{p}-\mathbf{a})\times(\mathbf{b}-\mathbf{a}).\]
对三角形 \((\mathbf{a},\mathbf{b},\mathbf{c})\)，其重心坐标可取
\[\lambda_a = \frac{E_{bc}(\mathbf{p})}{E_{bc}(\mathbf{a})},\quad
  \lambda_b = \frac{E_{ca}(\mathbf{p})}{E_{ca}(\mathbf{b})},\quad
  \lambda_c = \frac{E_{ab}(\mathbf{p})}{E_{ab}(\mathbf{c})}.\]
性质：\(\lambda_a+\lambda_b+\lambda_c=1\)；若采用一致的边朝向，则 \(\lambda_i\ge 0\) 当且仅当 \(\mathbf{p}\) 在三角形内（含边界）。

### 点是否在三角形内（PIT）
固定边方向（如逆时针），若
\[E_{ab}(\mathbf{p}) \ge 0,\; E_{bc}(\mathbf{p}) \ge 0,\; E_{ca}(\mathbf{p}) \ge 0,\]
则 \(\mathbf{p}\) 在三角形内。严格大于对应“严格内部”。

### 属性插值
仿射插值：属性 \(q\) 在屏幕空间按重心插值 \(q(\mathbf{p})=\sum_i \lambda_i q_i\)。
透视校正插值：设裁剪空间 w 分量为 \(w_i\)，则
\[\tilde{q}_i = \frac{q_i}{w_i},\quad \tilde{r}_i = \frac{1}{w_i},\]
\[\tilde{q}(\mathbf{p}) = \sum_i \lambda_i\,\tilde{q}_i,\quad
  \tilde{r}(\mathbf{p}) = \sum_i \lambda_i\,\tilde{r}_i,\quad
  q(\mathbf{p}) = \frac{\tilde{q}(\mathbf{p})}{\tilde{r}(\mathbf{p})}.\]

### 背面剔除与一致填充
- 背面剔除：若 \(\operatorname{sign}(A_2)\) 与设定朝向不符，则跳过该三角形。
- Top-left 规则：边函数取 \(>0\)/\(\ge 0\) 的一致性避免共享边重复填充。

### 光栅化实现要点
- 使用三角形包围盒遍历像素；边函数可对相邻像素常量增量更新。
- 预计算每条边的步进增量与常数项，提高效率。
- 深度测试与模板测试在插值后进行；法线/颜色/纹理坐标等按上式插值。