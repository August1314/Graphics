## 作业三：曲线与曲面生成算法实现 —— 开发方案

### 一、项目背景与目标
- **基础工程**：基于 `webapp2` Web 版二维绘图系统，已有完备的图形绘制、撤销重做、JSON 序列化、算法模块（直线/圆/填充）等功能。
- **作业目标**：
  - 在现有光栅化框架上，**手工实现至少两种经典参数曲线算法**（如三次 Bézier、二/三次 B 样条）。
  - **实现至少一种参数曲面算法**（优先双三次 Bézier 曲面，或三边 Bézier 曲面）。
  - 支持控制点拖拽、曲线/曲面动态更新，并实现**平移/旋转/缩放**等交互。
  - 与作业二线段/圆/填充算法集成，形成可对比展示的完整绘图系统。

### 二、总体设计框架
- **1. 分层结构沿用 `webapp2` 现有架构**
  - `scripts/shapes/`：新增曲线与曲面图形类，负责几何与渲染（Canvas 矢量级别）。
  - `scripts/tools/`：新增控制点编辑与曲线/曲面创建工具，负责交互。
  - `scripts/algorithms/curve/`、`scripts/algorithms/surface/`：新增参数曲线/曲面光栅化算法，基于 `BaseAlgorithm` 和 `PixelRenderer`。
  - `scripts/core/serializer.js`：扩展 JSON 序列化/反序列化，兼容新图元。
  - `scripts/ui/algorithm-selector.js`：扩展算法选择 UI，使曲线/曲面算法可配置。

- **2. 曲线/曲面表示与渲染策略**
  - **数据层（Shape）**：曲线/曲面以“控制点集合 + 参数设置（阶数、节点向量、细分步数等）”进行存储。
  - **几何层**：
    - 曲线：内部提供 `evaluate(u)` 接口，支持 Bernstein 基函数或 de Casteljau / 样条基函数计算点。
    - 曲面：提供 `evaluate(u, v)` 接口，基于张量积 Bernstein 或三角域 Bernstein 计算点。
  - **渲染层**：
    - 在 `render(ctx)` 中按参数等分抽样，得到折线网格。
    - 折线段调用现有线段算法（如 Bresenham）或 Canvas 直接描边；曲面采用网格线显示 + 可选填充（利用扫描线/三元坐标）。

- **3. 交互与变换架构**
  - **控制点模式**：曲线/曲面图形在选中时显示控制网格点，可用选择/拖拽修改。
  - **全局变换**：通过已有 `BaseShape.move` + 自定义 `rotate(scale)` 方法，实现围绕中心的旋转/缩放。
  - **工具模式切换**：在 `main.js` 中注册新工具（Bezier 曲线工具、B 样条工具、曲面补丁工具、变换工具），并在 UI 中增加按钮/快捷键。

### 三、功能设计与内容规划

#### 3.1 参数曲线算法
- **1. 三次 Bézier 曲线（必做）**
  - **算法实现**：
    - 提供两种内部实现方式：  
      - Bernstein 基函数形式 \( B(t) = \sum_{i=0}^{3} P_i C_3^i t^i (1-t)^{3-i} \)。  
      - de Casteljau 递推算法，实现数值稳定的插值。
    - 在 `algorithms/curve/bezier.js` 中实现 `BezierCurveAlgorithm extends BaseAlgorithm`，支持：
      - 输入：控制点数组、步数、颜色等。
      - 输出：通过 `PixelRenderer.setPixel` 在离屏 Canvas 上绘制曲线像素。
  - **Shape 与 Tool**：
    - `shapes/bezier_curve.js`：保存控制点列表，`render(ctx)` 负责画控制多边形 + 采样曲线折线。
    - `tools/bezier_curve.js`：点击添加控制点，右键/双击结束；支持选中控制点并拖拽更新。

- **2. B 样条曲线（二/三次，任选一种必做）**
  - **算法实现**：
    - 在 `algorithms/curve/bspline.js` 中实现 `BSplineCurveAlgorithm`，支持均匀 B 样条：
      - 预设或自动生成节点向量。
      - 基于 Cox-de Boor 递归公式计算样条基函数。
    - 同样基于 `PixelRenderer` 将采样点光栅化。
  - **Shape 与 Tool**：
    - `shapes/bspline_curve.js`：属性包括控制点、阶数、节点向量等。
    - `tools/bspline_curve.js`：与 Bézier 工具交互类似，但在 UI 上标出“插值/逼近”特性。

- **3. 拓展（可选加分）**
  - 支持**分段 Bézier 曲线**（C¹ 连续），在 UI 中以“多段曲线”形式出现。
  - 提供“精度（步数）”滑块，实时控制采样密度与性能。

#### 3.2 参数曲面算法
- **1. 双三次 Bézier 曲面（推荐实现）**
  - **数学模型**：  
    - 控制网格 \( P_{ij}, i,j=0..3 \)，曲面点  
      \[
      S(u,v) = \sum_{i=0}^{3}\sum_{j=0}^{3} P_{ij} B_i^3(u) B_j^3(v),\quad u,v\in[0,1]
      \]
  - **实现方式**：
    - 在 `algorithms/surface/bezier_patch.js` 中实现 `BezierSurfaceAlgorithm`：  
      - 输入：4×4 控制点网格、细分步数、渲染模式（网格/填充）。  
      - 输出：生成网格线段集合，用线段算法渲染；填充模式可将每个网格小四边形剖分为两个三角形，调用扫描线/三元坐标填充。
  - **Shape 与 Tool**：
    - `shapes/bezier_surface.js`：内部以二维数组存控制点；`render(ctx)` 支持：
      - 网格线显示：画控制网格 + 曲面网格。
      - 曲面填充：调用算法生成的三角形集，使用已有填充算法。
    - `tools/bezier_surface.js`：
      - 鼠标在画布上拖出一个矩形作为初始域，自动生成规则 4×4 控制点网格。  
      - 支持点击控制点并拖拽，实现“拉伸”曲面。

- **2. 三边 Bézier 曲面（可选加分）**
  - 在三角形参数域上定义控制点并实现三角域 Bernstein 基函数，文件如 `algorithms/surface/tri_bezier.js`、`shapes/tri_bezier_surface.js`。

#### 3.3 交互与变换功能
- **1. 平移 / 旋转 / 缩放**
  - 在曲线与曲面 Shape 中实现：
    - `move(dx, dy)`：沿用 `BaseShape.move`。
    - `rotate(angle, cx, cy)`：以中心点或指定点为旋转中心，对所有控制点做二维旋转变换。
    - `scale(sx, sy, cx, cy)`：围绕中心点缩放控制点。
  - 新增 `tools/transform.js` 或在选择工具中集成：
    - 通过拖拽边框控制点实现缩放，通过旋转手柄实现旋转。

- **2. 算法选择与可视化调试**
  - 扩展 `ui/algorithm-selector.js`：
    - 为曲线工具添加“算法下拉选择”：Bézier / B 样条。
    - 曲面工具可切换“网格显示 / 曲面填充 / 控制网格可见性”。
  - 在界面上实时显示当前算法名称、控制点数、采样点数等统计信息（使用 `BaseAlgorithm.stats`）。

- **3. 与作业二算法的对比展示**
  - 在 UI 中增加“对比模式”：同时绘制
    - 参数曲线 vs 由直线段逼近的折线；
    - 参数曲面网格 vs 多边形填充效果。

### 四、模块与文件规划
- **算法层**
  - `scripts/algorithms/curve/bezier.js`：三次 Bézier 曲线算法。
  - `scripts/algorithms/curve/bspline.js`：B 样条曲线算法。
  - `scripts/algorithms/surface/bezier_patch.js`：双三次 Bézier 曲面算法。
  - （可选）`scripts/algorithms/surface/tri_bezier.js`：三边 Bézier 曲面。
  - 扩展 `scripts/algorithms/factory.js`：
    - 新增 `curveAlgorithms`、`surfaceAlgorithms` 字典及对应 `createCurveAlgorithm` / `createSurfaceAlgorithm` 工厂方法。

- **图形层（shapes）**
  - `shapes/bezier_curve.js`、`shapes/bspline_curve.js`：继承 `BaseShape`，实现 `render`、`getBounds`、`setCenter`、`fromDict`。
  - `shapes/bezier_surface.js`（及可选 `tri_bezier_surface.js`）：用于曲面可视化。
  - 在 `core/serializer.js` 中注册上述新类型，支持保存/加载。

- **工具层（tools）**
  - `tools/bezier_curve.js`：控制点创建/编辑、曲线绘制。
  - `tools/bspline_curve.js`：B 样条工具。
  - `tools/bezier_surface.js`：曲面控制网格创建与编辑。
  - `tools/transform.js`（或增强 `select.js`）：实现平移/旋转/缩放交互。
  - 在 `main.js` 中注册工具，并在 UI 中配置按钮/快捷键（如 `9` = Bézier 曲线，`0` = 曲面工具）。

- **UI 与配置**
  - 扩展 `config.js`：增加默认曲线/曲面参数（采样步数、最大控制点数等）。
  - 扩展 `ui/algorithm-selector.js` 和界面布局，在 `draw.html` 中加入曲线/曲面算法选择与显示区域。

### 五、开发计划（时间与步骤）
- **阶段 1：熟悉与环境搭建（0.5 天）**
  - 熟悉 `webapp2` 架构：阅读 `main.js`、`core/canvas.js`、`shapes/*`、`tools/*`、`algorithms/*`。
  - 搭建本地开发环境，确认可正常运行 `draw.html`，并通过 `verify.sh`/手动测试。

- **阶段 2：参数曲线实现（2 天）**
  - 完成 `BezierCurveAlgorithm`、`BSplineCurveAlgorithm` 的数学与光栅化实现。
  - 新增 `BezierCurveShape`、`BSplineCurveShape` 与对应工具，实现控制点拖拽与实时更新。
  - 在 UI 中接入算法选择与参数调节（步数、阶数等），进行功能测试。

- **阶段 3：参数曲面实现（2 天）**
  - 实现 `BezierSurfaceAlgorithm` 与 `BezierSurfaceShape`，支持网格线显示。
  - 在曲面工具中实现 4×4 控制网格的初始化与交互编辑。
  - 实现“曲面填充”模式，结合已有填充算法完成基本光栅化。

- **阶段 4：变换与交互优化（1 天）**
  - 在曲线/曲面形状上实现旋转/缩放方法，并在工具中打通交互流程。
  - 优化控制点显示、选中高亮、快捷键、撤销/重做逻辑。
  - 与作业二算法联调，对比展示效果。

- **阶段 5：测试与文档（0.5–1 天）**
  - 完成边界情况测试（控制点过少/重合、极端参数、窗口缩放等）。
  - 撰写实验报告：算法推导、实现细节、关键代码截图、效果截图与性能分析。
  - 整理代码注释与 README 中的作业三说明。

### 六、风险与应对
- **数学实现复杂度**：提前推导并用小脚本验证 Bézier/B 样条/曲面公式，必要时先用 Canvas 矢量绘制，再迁移到像素级算法。
- **性能问题（高采样 + 大画布）**：通过步数上限、动态步数（随曲率调整）与 `requestAnimationFrame` 渐进绘制降低卡顿。
- **交互复杂度（控制点多、变换叠加）**：严格区分“编辑控制点模式”和“整体变换模式”，并在 UI 上给出明显提示。

上述方案既对接了 `webapp2` 的现有架构，又覆盖了作业三对曲线/曲面生成、光栅化与交互的全部要求，可在实现过程中根据时间与精力对可选部分（多段曲线、三边 Bézier 曲面等）进行取舍或加分拓展。


