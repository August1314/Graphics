### 开发规划（Python + Conda + 商用品质库）

- **技术路线（桌面端优先）**：PySide6（Qt for Python, 官方支持，稳定、跨平台、商用品质）+ Qt Graphics View 框架（`QGraphicsScene/View` + 自定义 `QGraphicsItem`）+ Pydantic v2（数据模型）+ QUndoStack（撤销/重做）+ Qt 原生导出（PNG/SVG）
- **理由**：Qt 在交互与绘制性能、窗口组件、国际化、撤销/重做、文件对话框、图像/矢量导出等方面一体化最强；PySide6 为官方绑定，许可证与稳定性适合课程与商用；Qt 自带高质量文本/图形渲染与事件系统，避免重复造轮子。

---

### 环境与依赖

- **创建 Conda 环境（名称：pnt）**
```bash
conda create -y -n pnt python=3.11
conda activate pnt
# 关键依赖（优先 conda-forge，安装更稳）
conda install -y -c conda-forge pyside6 pydantic pytest
# 可选增强（几何/导出/性能）：
# Shapely：精准几何与布尔运算（命中测试/加分项）
conda install -y -c conda-forge shapely
# Numpy：数据处理（如批量点/矩阵变换）
conda install -y -c conda-forge numpy
```

- **核心依赖说明**
  - **PySide6**: UI、画布、事件、撤销栈、导出、国际化
  - **Pydantic v2**: 数据模型与 JSON 序列化/校验
  - **Shapely（可选）**: 复杂几何/布尔运算/精确命中（加分）
  - **NumPy（可选）**: 批量坐标变换/性能优化
  - **PyTest**: 单元测试

---

### 项目结构（建议）

```
Graphics/
  app/
    main.py                     # 入口：创建 QApplication，加载主窗体
    ui/
      main_window.py            # 主窗口（菜单/工具栏/属性面板/状态栏）
      toolbar.py                # 工具栏（选择/点/线/矩形/圆/多边形）
      property_panel.py         # 属性面板（颜色/线宽/线型/填充/透明度）
      canvas_view.py            # 继承 QGraphicsView，处理缩放/平移/坐标系
      canvas_scene.py           # 继承 QGraphicsScene，集中绘制/命中调度
    core/
      document.py               # Document/Canvas 数据模型（Pydantic）
      shapes/
        base_item.py            # QGraphicsItem 基类 + Shape 接口绑定
        point_item.py
        line_item.py
        rect_item.py
        circle_item.py
        polygon_item.py
      tools/
        base_tool.py            # 工具接口：onDown/onMove/onUp/cancel
        select_tool.py
        point_tool.py
        line_tool.py
        rect_tool.py
        circle_tool.py
        polygon_tool.py
      commands/                 # 撤销/重做命令（QUndoCommand）
        add_shape_cmd.py
        move_shape_cmd.py
        delete_shape_cmd.py
        update_style_cmd.py
      selection.py              # 选择模型与多选框
      styles.py                 # 颜色/线宽/线型/透明度
      hit_test.py               # 命中测试（包围盒优先，必要时精确）
      transforms.py             # 画布与场景坐标变换
    io/
      serializer.py             # JSON 存储/加载（Pydantic 模型）
      exporter.py               # 导出 PNG/SVG（QImage/QSvgGenerator）
    i18n/
      zh_CN.ts                  # 可选：国际化
  tests/
    test_models.py
    test_serializer.py
    test_hit_test.py
  samples/
    example_scene.json
  README.md
```

---

### 需求到实现映射

- **图形绘制**：每种图元一个 `QGraphicsItem` 子类；工具切换驱动对应的绘制状态机
- **图形编辑**：`select_tool` + 橡皮框 + 拖拽移动（支持多选）；命令写入 `QUndoStack`
- **缩放/平移**：`canvas_view` 捕捉滚轮/空格+拖拽；显示缩放比例（状态栏）
- **属性设置**：`property_panel` 双向绑定选中项样式（颜色/线宽/线型/填充/透明度）
- **文件存储与加载**：`serializer` 使用 Pydantic 严格校验；异常弹窗与保护
- **导出（加分）**：`exporter` 使用 Qt 渲染到 PNG/SVG
- **撤销/重做（加分）**：`commands` 基于 `QUndoStack`

---

### 里程碑与时间分配（与要求.md 对齐）

- **M1（第1周）**：脚手架 + 数据模型 + 画布基础
  - 完成 `QMainWindow` 框架、`QGraphicsView/Scene`、坐标变换、状态栏坐标/缩放显示
  - 建立 Pydantic `Document/Shape` 模型与 JSON Schema
- **M2（第2周）**：图元绘制 + 选择/拖动 + 属性联动
  - 点/线/矩形/圆/多边形工具完成；选择/多选与拖拽；属性面板双向绑定
- **M3（第3周）**：文件存储/加载 + 视图缩放/平移完善 + 稳定性
  - 完成 `save/load`；异常处理；性能优化（脏矩形、包围盒命中）
- **M4（第4周）**：加分项 + 测试与文档
  - 撤销/重做、导出 SVG/PNG、网格与吸附（可选）；完善测试与 README

---

### 核心设计细节

- **Shape 抽象**
  - 运行时：`QGraphicsItem` 子类承担渲染与交互；保持与数据模型同步
  - 数据：Pydantic `ShapeBase` 多态模型，字段参考 `要求.md` 数据模型
  - 命中：先包围盒（`QGraphicsItem.boundingRect`），必要时用路径精确命中

- **工具状态机**
  - 接口：`onPointerDown/Move/Up/cancel`
  - 选择工具：框选、单击选择、Shift 多选
  - 绘制工具：空闲→绘制中→完成；支持 Esc 取消

- **撤销/重做**
  - Qt `QUndoStack` + `QUndoCommand`：新增、移动、删除、样式变更
  - 菜单/快捷键绑定：Cmd/Ctrl+Z / Shift+Cmd/Ctrl+Z

- **文件与导出**
  - `serializer`: Pydantic 校验 + 版本号字段；异常提示不丢失场景
  - `exporter`: `QImage/QPainter` → PNG；`QSvgGenerator` → SVG

- **性能与稳定性**
  - 脏矩形重绘（Qt 自带优化）；`ItemIsMovable/Selectable` 合理使用
  - 大量图元时简化命中测试、延迟绘制选框/控制柄

---

### 验收与测试

- **单元测试**：模型校验、序列化/反序列化、命中测试、命令可逆性
- **集成测试（手工）**：主流程“绘制-编辑-保存-加载”；多平台（macOS/Win）
- **示例文件**：`samples/example_scene.json`；导出 PNG/SVG 示例

---

### 风险与应对

- **几何复杂度**：先用包围盒/Qt 路径命中；若需要布尔运算再引入 Shapely
- **跨平台字体/渲染差异**：统一 DPI 策略（`devicePixelRatio`），减少依赖系统字体
- **时间超限**：优先闭环主流程，M4 加分项可裁剪

---
